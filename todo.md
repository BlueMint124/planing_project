# AI 단체 여행 일정 생성기 TODO

이 문서는 제품 개발의 실행 백로그이자 진행 상태 기록이다.
작업을 시작할 때 해당 항목을 `[ ]`에서 `[~]`로, 완료 후 검증까지 끝났을 때 `[x]`로
변경한다. 새 요구사항이나 결정 사항은 코드보다 먼저 이 문서에 반영한다.

## Status Legend

- `[ ]`: 시작 전
- `[~]`: 진행 중
- `[x]`: 구현 및 검증 완료
- `[!]`: 외부 결정 또는 차단 요소 필요

## 0. 개발 착수 결정 게이트

- [x] 기술 스택을 결정한다.
  - 프론트엔드 프레임워크와 언어
  - 백엔드 런타임과 API 프레임워크
  - 데이터 저장소
  - 테스트, 린트, 포맷 도구
  - 배포 대상
- [x] AI 모델과 생성 방식을 결정한다.
  - 구조화 출력 스키마
  - 프롬프트 버전 관리 방식
  - 타임아웃, 재시도, 실패 처리 정책
- [x] 지도 및 장소 데이터 제공자를 결정한다.
  - 지역 검색, 장소 검색, 좌표, 이동 시간 제공 범위
  - 호출 제한, 비용, 키 관리 방식
- [ ] 예약 링크 제공 범위를 결정한다.
  - 숙소, 식당, 액티비티 중 MVP 포함 대상
- [ ] 공유 결과 정책을 결정한다.
  - 저장 기간
  - 공개 URL 접근 방식
  - 멤버 선호 정보 노출 범위
- [ ] 비로그인 재생성 횟수 제한을 결정한다.
- [ ] 비용 추정 방식과 허용 정확도 기준을 결정한다.
- [ ] 에러 추적, 로그, 모니터링 도구를 결정한다.

**완료 조건:** 모든 결정이 문서화되고, 구현에 필요한 외부 서비스 자격 증명과 개발
환경 준비 방법이 정의되어 있다.

## 1. 저장소 하네스와 기본 골격

- [x] 루트 `AGENTS.md`를 작성한다.
- [x] 루트 `todo.md`를 작성한다.
- [x] GitHub 원격 저장소 `origin`을 연결한다.
- [x] 기능 단위 커밋 및 푸시 규칙을 문서화한다.
- [x] 서로 다른 PC의 Codex가 맥락을 공유하는 협업 가이드를 작성한다.
- [x] 담당자, 브랜치, 상태, 의존성을 관리하는 작업 보드를 작성한다.
- [x] 공통 계약 문서와 기능별 handoff 문서 구조를 작성한다.
- [x] Pull Request 템플릿을 작성한다.
- [x] 기술 스택과 데모 모드 운영 방침을 문서화한다.
- [x] 토큰 예산을 고려한 기능별 담당 범위를 문서화한다.
- [x] 주 개발자와 협업자용 Codex 시작 프롬프트를 작성한다.
- [x] 데스크톱 Smart Dashboard UI 디자인 방향을 확정한다.
- [x] 화면별 UI 시각 참조와 개발 명세를 작성한다.
- [ ] 원본 기능 명세서를 저장소 내부 제품 명세 문서로 옮긴다.
- [x] 기술 스택 결정 후 저장소 디렉터리 구조를 만든다.
- [x] 개발, 테스트, 린트, 타입 검사, 빌드 명령을 정의한다.
- [x] 환경 변수 예시 파일과 비밀키 관리 규칙을 추가한다.
- [ ] CI에서 테스트, 린트, 타입 검사, 빌드를 실행한다.
- [x] 로컬 개발 시작 방법과 검증 명령을 문서화한다.

**완료 조건:** 새 에이전트 또는 개발자가 저장소 문서만 읽고 앱을 실행하고 기본 검증을
수행할 수 있다.

## 2. 도메인 모델과 API 계약

- [x] 여행 생성 요청 모델을 정의한다.
  - `destination`: 1~50자 지역 또는 도시명
  - `duration`: 당일치기, 1박2일, 2박3일, 직접 선택
  - `budgetPerPerson`: 0보다 큰 정수
  - `groupSize`: 2~10 정수
  - `styles`: 허용 목록에서 최소 1개
  - `members`: 최대 10명의 이름, 선호, 비선호 태그
- [x] 여행 생성 결과 모델을 정의한다.
  - `tripId`, 비용 요약, 일정별 장소, 좌표, 이동 시간, 예약 링크
- [x] 오류 응답 모델을 정의한다.
  - `errorCode`, 사용자 메시지, `requestId`
- [x] 상태 모델과 허용 전이를 정의한다.
  - `draft`, `validating`, `generating`, `generated`, `failed`, `shared`
- [x] 요청 및 응답 스키마 검증 테스트를 작성한다.
- [x] API 계약 문서를 생성하거나 갱신한다.

**완료 조건:** 프론트엔드와 백엔드가 동일한 타입과 검증 규칙을 사용하며 잘못된 입력과
잘못된 AI 출력이 경계에서 차단된다.

## 3. 여행 조건 입력 플로우

- [ ] 모바일 우선 단계형 입력 화면을 구현한다.
- [ ] 지역 검색 입력을 구현한다.
- [ ] 여행 기간 preset과 직접 선택을 구현한다.
- [ ] 1인당 예산 입력을 구현한다.
- [ ] 2~10명 인원 선택을 구현한다.
- [ ] 여행 스타일 다중 선택을 구현한다.
  - 맛집, 카페, 자연, 액티비티, 쇼핑, 역사/문화, 힐링, 사진, 아이동반, 부모님동반
- [ ] 선택형 멤버별 선호 및 비선호 입력을 구현한다.
- [ ] 다음, 이전, 일정 생성 버튼 동작을 구현한다.
- [ ] 필수값 누락과 잘못된 형식의 인라인 에러를 구현한다.
- [ ] 입력 시작 전 예시 일정 또는 추천 입력값 빈 상태를 구현한다.
- [ ] 입력 플로우 단위 및 사용자 흐름 테스트를 작성한다.

**완료 조건:** 유효한 입력만 생성 요청으로 전달되고, 사용자는 오류를 필드 단위로
이해하고 수정할 수 있다.

## 4. 외부 장소 데이터 연동

- [x] 장소 제공자 어댑터 인터페이스를 정의한다.
- [x] 지역 및 장소 검색 연동을 구현한다.
- [x] 장소 좌표와 카테고리 정규화를 구현한다.
- [x] 장소 간 이동 시간 또는 거리 조회를 구현한다.
- [ ] 장소 상세 또는 예약 링크 생성을 구현한다.
- [x] 장소 데이터 부족 시 인접 지역 후보 확장을 구현한다.
- [ ] 외부 API 타임아웃, 재시도, 호출 제한 처리를 구현한다.
- [x] 외부 API를 대체하는 테스트 더블과 계약 테스트를 작성한다.

**완료 조건:** 장소 제공자를 교체할 수 있고, 외부 API 실패가 일정 생성 전체의
진단 가능한 실패 또는 안내 가능한 대체 결과로 처리된다.

## 5. AI 일정 생성 API

- [x] `POST /api/trips/generate` 엔드포인트를 구현한다.
- [x] 요청별 `requestId`를 생성하고 전파한다.
- [x] 입력 검증 실패 시 400 응답을 구현한다.
- [x] 장소 후보 수집과 정규화를 구현한다.
- [x] AI 생성 프롬프트와 구조화 출력 스키마를 구현한다.
- [x] 동선 효율, 예산 준수, 단체 취향 균형 우선순위를 반영한다.
- [x] 동일 카테고리 장소의 과도한 반복 제한을 구현한다.
- [x] 총 예상 비용과 1인당 예상 비용을 계산한다.
- [x] 예산 초과 상태와 안내를 생성한다.
- [x] AI 또는 외부 API 실패 시 500 오류 응답과 재시도 가능한 메시지를 구현한다.
- [ ] 동일 요청 처리 중복 또는 상태 충돌 시 409 응답을 구현한다.
- [x] 30초 응답 목표를 측정할 수 있는 타이밍 로그를 구현한다.
- [x] 정상, 예외, 실패 API 테스트를 작성한다.

**완료 조건:** 정상 요청은 명세의 성공 응답을 반환하고, 모든 실패는 `requestId`로
추적 가능하며 사용자에게 안전한 메시지를 제공한다.

## 6. 결과 화면과 사용자 동작

- [ ] 생성 중 화면과 "동선과 예산을 계산 중" 메시지를 구현한다.
- [ ] 생성 성공 후 결과 화면 이동을 구현한다.
- [ ] 비용 및 동선 요약을 구현한다.
- [ ] 일자별 추천 일정과 장소 카드를 구현한다.
- [ ] 지도 기반 동선 표시를 구현한다.
- [ ] 예산 초과 안내를 구현한다.
- [ ] AI 추천 정보 면책 안내를 구현한다.
- [ ] 다시 생성 버튼으로 동일 조건 재요청을 구현한다.
- [ ] 생성 실패 토스트와 재시도 동작을 구현한다.
- [ ] 외부 예약 링크를 안전한 새 탭 또는 외부 브라우저로 연다.
- [ ] 결과 화면 단위 및 사용자 흐름 테스트를 작성한다.

**완료 조건:** 사용자는 생성 결과의 일정, 동선, 비용을 이해하고 실패 후 재시도하거나
외부 링크로 이동할 수 있다.

## 7. 공유 결과 저장과 조회

- [ ] 공유용 결과 ID와 생성 결과 임시 저장을 구현한다.
- [ ] 공유 결과 조회 API 또는 라우트를 구현한다.
- [ ] 공유 URL 생성을 구현한다.
- [ ] 공유 버튼 사용 시 상태를 `shared`로 전이한다.
- [ ] 존재하지 않거나 만료된 공유 결과의 404 처리를 구현한다.
- [ ] 공유 결과에서 민감한 멤버 선호 정보가 노출되지 않도록 한다.
- [ ] 공유 흐름 테스트를 작성한다.

**완료 조건:** 공유 URL을 받은 사용자가 저장 기간 안에 결과를 조회할 수 있고, 불필요한
개인정보는 노출되지 않는다.

## 8. 관찰 가능성과 운영 준비

- [ ] 생성 실패, 외부 API 실패, 비정상 요청 로그를 구현한다.
- [ ] 로그에 `requestId`, 생성 시각, 입력 조건 요약, 응답 시간, 실패 코드를 포함한다.
- [ ] 로그에서 비밀키와 불필요한 개인정보를 제거한다.
- [ ] 에러 트래킹 도구를 연동한다.
- [ ] 생성 성공률, 실패율, 외부 API 실패, 응답 지연 지표를 정의한다.
- [ ] 실패율 임계치, 외부 API 연속 실패, 평균 응답 시간 초과 알림을 정의한다.
- [ ] 운영 확인 절차를 문서화한다.

**완료 조건:** 운영자가 생성 실패와 응답 지연을 감지하고 `requestId`로 원인을 추적할
수 있다.

## 9. QA와 배포 준비

- [ ] 정상 입력 시 기대 결과가 생성되는지 확인한다.
- [ ] 성공 후 결과 화면으로 이동하는지 확인한다.
- [ ] 지도와 동선 정보가 표시되는지 확인한다.
- [ ] 1인당 예상 비용과 총 예상 비용이 표시되는지 확인한다.
- [ ] 공유 URL이 생성되고 조회되는지 확인한다.
- [ ] 예약 링크 클릭 시 외부 페이지로 이동하는지 확인한다.
- [ ] 필수값 누락 시 에러가 표시되는지 확인한다.
- [ ] 잘못된 형식 입력이 차단되는지 확인한다.
- [ ] 외부 연동 실패가 처리되는지 확인한다.
- [ ] 장소 데이터 부족 시 대체 추천 또는 안내가 표시되는지 확인한다.
- [ ] 예산 초과 일정에 안내가 표시되는지 확인한다.
- [ ] 모바일 주요 해상도와 데스크톱 브라우저에서 핵심 흐름을 확인한다.
- [ ] 일정 생성 성공률과 30초 응답 목표를 확인한다.
- [ ] 배포 및 롤백 절차를 문서화한다.

**완료 조건:** 명세의 정상, 예외, 실패 테스트가 통과하고 주요 오류 로그 없이 배포
가능 기준을 충족한다.

## Decision Log

결정이 내려질 때 날짜, 결정, 이유, 영향 범위를 기록한다.

| 날짜 | 결정 | 이유 | 영향 범위 |
| --- | --- | --- | --- |
| 2026-06-04 | 루트 에이전트 지침 파일명을 `AGENTS.md`로 사용 | 에이전트 도구가 자동으로 발견하는 표준 파일명 사용 | 저장소 전체 |
| 2026-06-04 | 기능 완료 및 검증 후 현재 기능 브랜치를 원격에 푸시 | 기능별 이력을 보존하고 원격 백업과 협업 기준을 유지 | 개발 워크플로 |
| 2026-06-04 | `main` 반영은 Pull Request로만 진행 | 서로 다른 PC의 변경을 리뷰하고 충돌과 계약 영향을 확인 | 개발 워크플로 |
| 2026-06-04 | 저장소 문서와 PR을 Codex 공유 맥락으로 사용 | Codex 대화는 다른 PC에 자동 공유되지 않음 | 협업 하네스 |
| 2026-06-04 | Next.js 단일 저장소 풀스택과 Vercel 배포 사용 | 프론트엔드와 API를 한 저장소에서 관리해 배포와 협업 단순화 | 기술 스택 |
| 2026-06-04 | Supabase, OpenAI Structured Outputs, Google Places 및 Routes API 사용 | 공유 저장, 구조화 AI 결과, 장소 및 동선 기능 제공 | 외부 연동 |
| 2026-06-04 | `DEMO_MODE=true`에서 고정 제주 일정 반환 | 발표 시연 중 외부 API 장애와 예상치 못한 과금 방지 | 발표 운영 |
| 2026-06-04 | 제한 토큰 협업자는 계약이 고정된 UI 기능 담당 | 읽어야 하는 맥락과 충돌 위험을 줄임 | 기능 분담 |
| 2026-06-04 | 데스크톱 Smart Dashboard를 기본 UI로 사용 | 입력과 요약, 일정과 지도를 동시에 보여 발표와 정보 이해에 유리 | UI 디자인 |
| 2026-06-04 | 모바일은 핵심 기능을 유지하는 보조 반응형으로 대응 | 데스크톱 개발에 집중하면서 브라우저 접근성 유지 | UI 디자인 |
