# AGENTS.md

이 저장소는 **AI 단체 여행 일정 생성기**를 개발하기 위한 작업 공간이다.
이 문서는 에이전트가 저장소를 빠르게 이해하고, 일관된 방식으로 작업하도록 돕는
짧은 지도다. 세부 요구사항과 진행 상태는 아래 문서를 소스 오브 트루스로 사용한다.

## Repository Map

- `AGENTS.md`: 에이전트 작업 원칙과 저장소 지도
- `todo.md`: 제품 개발 백로그, 결정 사항, 진행 상태, 완료 조건
- `docs/collaboration/README.md`: 서로 다른 PC에서 작업하는 개발자와 Codex의 협업 흐름
- `docs/collaboration/work-board.md`: 현재 담당자, 브랜치, 상태, 의존성, PR 요약
- `docs/collaboration/feature-ownership.md`: 주 개발자와 제한 토큰 협업자의 기능 분담
- `docs/collaboration/prompts/`: 각 작업자가 Codex 세션을 시작할 때 사용하는 프롬프트
- `docs/design-docs/technology-stack.md`: 확정된 기술 스택과 데모 운영 방침
- `DESIGN.md`: UI 디자인 시스템과 공통 컴포넌트 규칙
- `docs/ui-design/screens.md`: 화면별 기능, 상태, 레이아웃, 완료 조건
- `docs/ui-design/assets/`: 승인된 데스크톱 UI 시각 참조
- `docs/contracts/`: 프론트엔드와 백엔드가 함께 사용하는 계약
- `docs/handoffs/`: 기능별 작업 맥락, 검증 결과, 남은 작업
- `.github/pull_request_template.md`: Pull Request에 남겨야 하는 공유 맥락
- `C:/Users/USER/Desktop/AI_여행계획생성기_기능명세서.md`: 원본 기능 명세서
- GitHub 원격 저장소: `https://github.com/BlueMint124/planing_project.git`

프로젝트 구조, 기술 스택, 실행 명령이 확정되면 이 지도를 즉시 갱신한다.
장기적으로는 제품 명세, 설계 문서, 실행 계획을 `docs/` 아래에 두고 이 문서에서는
링크만 제공한다.

## Product Intent

사용자가 단체 여행의 지역, 기간, 1인당 예산, 인원, 여행 스타일, 멤버별 선호를
입력하면 동선 효율, 예산 준수, 단체 취향 균형을 고려한 여행 일정을 생성한다.

MVP의 핵심 결과는 다음과 같다.

- 추천 일정과 장소별 방문 순서
- 지도 좌표와 이동 동선
- 총 예상 비용과 1인당 예상 비용
- 공유 가능한 결과 URL
- 외부 상세 또는 예약 링크

MVP 범위에 앱 내 결제, 실예약 확정, 실시간 예약 가능 여부 보장, 로그인 기반 협업은
포함하지 않는다.

## Product Rules

- 여행 인원은 2명 이상 10명 이하의 정수다.
- 여행 기간은 당일치기부터 최대 3박 4일까지 허용한다.
- 예산은 0보다 큰 1인당 예산이다.
- 여행 스타일은 최소 1개를 선택해야 한다.
- 일정 생성 우선순위는 동선 효율, 예산 준수, 단체 취향 균형 순이다.
- 장소 데이터가 부족하면 인접 지역까지 후보를 확장한다.
- 동일 카테고리 장소가 과도하게 반복되지 않도록 제한한다.
- AI 결과는 추천 정보이며 실제 가격, 영업시간, 예약 가능 여부는 외부 링크에서
  확인해야 한다.
- 일정 생성 요청은 30초 이내 응답을 목표로 한다.

## State Model

일정 생성 흐름은 다음 상태를 사용한다.

`draft` -> `validating` -> `generating` -> `generated` -> `shared`

검증 실패 시 `validating`에서 `draft`로 돌아가고, 생성 실패 시 `generating`에서
`failed`로 전이한다.

- `draft`: 입력 수정 가능
- `validating`: 입력값 검증 중
- `generating`: 입력 수정 불가, 생성 대기 또는 취소 가능
- `generated`: 공유, 재생성, 예약 링크 이동 가능
- `failed`: 동일 조건 재시도 또는 입력 수정 가능
- `shared`: 공유 동작이 수행된 생성 결과

상태 전이는 명시적으로 구현하고, UI와 API가 서로 다른 상태를 추정하지 않도록 한다.

## API Contract

핵심 생성 API는 `POST /api/trips/generate`다.

요청의 주요 필드는 `destination`, `duration`, `budgetPerPerson`, `groupSize`, `styles`,
`members`다. 성공 응답은 `tripId`, 비용 `summary`, 방문 순서가 담긴 `route`를
제공한다. 실패 응답은 `errorCode`, 사용자 메시지, `requestId`를 제공한다.

API 계약을 변경할 때는 프론트엔드 타입, 서버 검증, 테스트, 문서를 같은 작업에서
함께 갱신한다.

## Working Agreement

모든 작업은 다음 루프를 따른다.

1. 작업 시작 전에 `AGENTS.md`, `todo.md`, 협업 작업 보드, 관련 계약, handoff 문서,
   `DESIGN.md`, 관련 화면 명세, 기존 코드를 읽는다.
2. 요구사항이 불명확하면 추측으로 외부 서비스나 비즈니스 정책을 고정하지 않는다.
3. 작업 범위를 작고 검증 가능한 단위로 나누고 `todo.md`의 상태를 갱신한다.
4. 기능 또는 버그 수정은 가능하면 실패하는 테스트를 먼저 작성한다.
5. 기존 구조와 패턴을 우선하며, 관련 없는 리팩터링을 섞지 않는다.
6. 사용자 입력, 외부 API 응답, AI 생성 결과를 신뢰하지 않고 경계에서 검증한다.
7. 실패 시 사용자에게 재시도 가능한 메시지를 제공하고, 서버에는 진단 가능한 로그를
   남긴다.
8. 작업 완료 전 관련 테스트, 타입 검사, 린트, 빌드를 실행한다.
9. 새로 알게 된 제약, 실행 명령, 구조 변경은 같은 작업에서 문서에 반영한다.
10. 기능 하나가 완료 조건을 충족하면 논리적 커밋을 만들고 원격 저장소에 푸시한다.

## Version Control Workflow

- 원격 저장소 이름은 `origin`을 사용한다.
- `main`에 직접 커밋하거나 직접 푸시하지 않는다.
- 새 기능 작업은 `codex/<feature-name>` 형식의 브랜치에서 진행한다.
- 기능 브랜치를 만들기 전에 `main`에서 `git pull --ff-only origin main`을 실행한다.
- 작업 시작 시 `docs/collaboration/work-board.md`에 담당자, 브랜치, 상태, 의존성을
  등록한다.
- 커밋 하나는 독립적으로 이해하고 검증할 수 있는 하나의 논리적 변경만 포함한다.
- 기능 코드와 해당 테스트는 같은 커밋에 포함한다.
- 커밋 메시지는 `feat:`, `fix:`, `docs:`, `test:`, `chore:`, `refactor:` 접두사를
  사용한다.
- 기능이 구현되고 관련 검증이 통과하면 커밋 후 즉시 현재 브랜치를 `origin`에
  푸시한다.
- 검증에 실패한 기능이나 미완성 상태를 완료된 기능으로 표시하거나 푸시하지 않는다.
- 강제 푸시는 사용하지 않는다.
- 공유 브랜치의 기존 커밋을 재작성하지 않는다.
- 공통 파일을 수정하기 전에 작업 보드의 `Shared File Lock`에 기록한다.
- API, 데이터 타입, 상태 모델 변경은 `docs/contracts/`에 기록한다.
- 푸시 전 `todo.md`, 작업 보드, 기능별 handoff 문서를 갱신한다.
- 기능 브랜치는 Pull Request를 통해서만 `main`에 반영한다.
- Pull Request에는 계약 변경, 다른 작업에 미치는 영향, 검증 명령과 결과를 기록한다.

## Multi-Agent Context Sharing

Codex 대화 내용은 다른 PC에 자동으로 전달되지 않는다. 다른 개발자와 Codex가 알아야
하는 맥락은 저장소에 기록하고 커밋해야 한다.

- 현재 작업의 소유권과 의존성은 `docs/collaboration/work-board.md`에 기록한다.
- 공통 API와 데이터 모델의 기준은 `docs/contracts/`에 기록한다.
- 구현 결정, 주요 파일, 검증 결과, 남은 작업은 `docs/handoffs/<feature-name>.md`에
  기록한다.
- 코드 리뷰와 병합 판단에 필요한 내용은 Pull Request에 기록한다.
- 대화에서만 합의한 내용은 결정으로 간주하지 않는다.
- 상세 절차는 `docs/collaboration/README.md`를 따른다.

## Engineering Guardrails

- 비밀키와 자격 증명을 저장소에 커밋하지 않는다.
- 외부 API 호출은 어댑터 경계로 격리해 대체 및 테스트가 가능하도록 한다.
- AI 출력은 구조화된 스키마로 검증한 뒤 사용한다.
- 외부 링크는 허용된 URL 스킴과 안전한 새 탭 동작을 사용한다.
- 공유 결과에는 불필요한 개인정보나 민감한 멤버 선호 정보를 노출하지 않는다.
- 로그에는 `requestId`, 생성 시각, 입력 조건 요약, 응답 시간, 실패 코드를 남기되
  원본 개인정보와 비밀값은 남기지 않는다.
- 생성 재시도는 동일 요청 중복과 상태 충돌을 고려한다.
- 모바일 웹을 우선하되 데스크톱 브라우저에서도 핵심 흐름이 동작해야 한다.

## Definition Of Done

작업은 다음 조건을 충족해야 완료로 표시할 수 있다.

- 요구사항과 수용 기준이 코드 또는 문서로 추적 가능하다.
- 정상, 예외, 실패 흐름에 필요한 테스트가 추가되고 통과한다.
- 사용자에게 보이는 로딩, 빈 상태, 성공, 실패 상태가 처리된다.
- 외부 연동 실패와 AI 생성 실패가 관찰 가능하다.
- 관련 문서와 `todo.md`가 현재 상태를 반영한다.
- 실행 가능한 검증 명령과 결과를 작업 보고에 남긴다.
- 기능 단위 커밋이 원격 저장소에 푸시되어 있다.
- 기능별 handoff 문서와 Pull Request가 다른 작업자가 이해할 수 있는 상태다.

## Pending Decisions

다음 항목은 구현 전에 `todo.md`에서 결정하고 기록한다.

- 예약 링크 제공 범위
- 공유 결과 저장 기간
- 비로그인 재생성 횟수 제한
- 비용 추정 방식과 정확도 기준
- 에러 추적 및 모니터링 도구
